import React from "react";

const StepOtp = ({ onNext }) => {
    return (
        <>
            <div>
                OTP Component
            </div>
            <button onClick={onNext}>Next</button>
        </>
    );
}
export default StepOtp;